.navbar {
  background-color: lightseagreen;
}

.navbar-container {
  max-width: 250px;
  margin: auto;
  display: flex;
  justify-content: space-between;
}

.navbar-item {
  padding: 10px;
  color: white;
}

.navbar-item:hover {
  background-color: darkolivegreen;
}

body {
  font-family: Arial;
  margin: 0;
  background-color: rgb(206, 234, 211);
}

.hero {
  background-color: rgb(61, 185, 115);
  padding: 25px;
}

.title {
  margin: 0;
  font-size: 50px;
  text-align: center;
}
/* This centers the textThis centers the textThis centers the textThis centers the textThis centers the textThis centers the textThis centers the textThis centers the text */
.container {
  max-width: 1000px; 
  margin: auto;
  padding: 20px;
  background-color: white;
  flex-direction: column;
}

.image-grid {
  display: flex;
  justify-content: space-between;
  gap: 10px;
  margin-bottom: 20px;
}

.image-grid img {
  width: 20%;
  border-radius: 5px;
}

.label {
  font-weight: bold;
}

.input, .select select {
  width: 95%;
  padding: 10px;
  margin-top: 10px;
  margin-bottom: 10px;
  font-size: 1em;
}

.button {
  padding: 10px 15px;
  border: none;
}

.button.is-primary {
  background-color: #3273dc;
}

.button.is-danger {
  background-color: #ff3860;
}

#message {
  display: none;
  margin: 20px;
  font-weight: bold;
  padding: 8px;
  border-radius: 4px;
  border-color: aquamarine;
}

.field-parent-columns {
  display: flex;
  justify-content: space-between;
}

.field-parent-columns .field {
  width: 45%;
}

.image-top-left,
.image-top-right {
  flex-direction: column;
  gap: 10px;
}

.image-bottom {
  margin-top: 20px;
  text-align: center;
}

.image-top-left img:nth-of-type(1),
.image-top-right img:nth-of-type(2) {
  width: 200px; 
}

.image-top-left img:nth-of-type(2),
.image-top-right img:nth-of-type(1) {
  width: 200px; 
}

.image-bottom img {
  width: 300px; 
}
